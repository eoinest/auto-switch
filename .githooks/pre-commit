#!/bin/sh
set -eu
cd "$(git rev-parse --show-toplevel)"
python3 tools/check_staged_secrets.py
if ! command -v gitleaks >/dev/null 2>&1; then
  echo "Commit blocked: install Gitleaks to scan staged files." >&2
  exit 1
fi
exec gitleaks git --staged --redact --no-banner --ignore-gitleaks-allow .
